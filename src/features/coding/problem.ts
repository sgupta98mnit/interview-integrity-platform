export const problemStatement = {
  title: "Two Sum",
  description:
    "Given an array of integers and a target, return indices of the two numbers such that they add up to target.",
  starterCode: `export function solve(nums: number[], target: number): [number, number] | null {\n  // Return [i, j] if nums[i] + nums[j] === target\n  // Return null if no pair exists\n  return null;\n}`,
};
